<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.extrainch.ngao">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission
        android:name="android.permission.READ_PRIVILEGED_PHONE_STATE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ngao">
        <activity
            android:name=".ui.loanadvanced.AdvancedLoanActivity"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity
            android:name=".ui.loanpay.PayLoanActivity"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity
            android:name=".ui.loanrequest.RequestLoanActivity"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".ui.register.RegisterActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".ui.splash.CheckUserActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
    </application>

</manifest>